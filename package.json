{
  "name": "screen-assistant",
  "version": "0.22.0",
  "private": true,
  "description": "Plataforma SaaS de agentes visuais com autenticação, roteamento progressivo, Gemini, voz e PWA.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
